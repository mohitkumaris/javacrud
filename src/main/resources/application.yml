spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/studentdb
    username: postgres
    password: postgres1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081